services:
  database_administration:
    image: phpmyadmin/phpmyadmin:5
    profiles:
      - local
    environment:
      PMA_HOSTS: database,queue_db
    labels:
      - traefik.enable=true
      - traefik.http.routers.database_administration.rule=Host(`pma.${DOMAIN}`)
